### Analyze uploaded resume with a manual session id
# Select the `local` environment in the REST Client extension and set
# `sessionId` plus `jobDescription` in `rest-client.env.json`.
POST {{baseUrl}}/api/analyze
Content-Type: application/json

{
  "sessionId": "{{sessionId}}",
  "jobDescription": "{{jobDescription}}"
}
